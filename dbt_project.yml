name: 'bookstore_dbt'
version: '1.0.0'
config-version: 2

profile: 'bookstore_dbt'

model-paths:  ["models"]
seed-paths:   ["seeds"]
test-paths:   ["tests"]
macro-paths:  ["macros"]

target-path:  "target"
clean-targets: ["target", "dbt_packages"]

models:
  bookstore_dbt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

seeds:
  bookstore_dbt:
    +schema: raw